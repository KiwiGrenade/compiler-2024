PROGRAM IS
    a,b,c
IN
    a:=0;
    c:=a*30;
    IF c=0 THEN
        WRITE 1;
    ELSE
        WRITE 0;
    ENDIF

    a:=30;
    c:=a*0;
    IF c=0 THEN
        WRITE 1;
    ELSE
        WRITE 0;
    ENDIF
    
    a:=0;
    c:=a*0;
    IF c=0 THEN
        WRITE 1;
    ELSE
        WRITE 0;
    ENDIF

    a:=1;
    c:=a*1;
    IF c=1 THEN
        WRITE 1;
    ELSE
        WRITE 0;
    ENDIF

    a:=0;
    c:=30*a;
    IF c=0 THEN
        WRITE 1;
    ELSE
        WRITE 0;
    ENDIF

    a:=30;
    c:=0*a;
    IF c=0 THEN
        WRITE 1;
    ELSE
        WRITE 0;
    ENDIF
    
    a:=0;
    c:=0*a;
    IF c=0 THEN
        WRITE 1;
    ELSE
        WRITE 0;
    ENDIF
    
    a:=1;
    c:=a*1;
    IF c=1 THEN
        WRITE 1;
    ELSE
        WRITE 0;
    ENDIF
END
